here should be general rules how to work with project in different modes.
this file is for python_tool_technical_lead role.

On investigation mode
need to look up for existing solutions, open source and free, enterprice solution.
need to learn about domain. auderstand the scope.
on the end should be investigation report in report folder (investigation\_{timestamp})

on review mode
need to review current project find week places, unclear decision, unconcistency on the project.
on the end should be investigation report in report folder (review\_{timestamp})

on planning mode
need to develop the roadmap to get the result. on each step should be working product with new feature.
(plan\_{timestamp})

on prototyping node
need to produce simple working script that solve the task.
some minimal set for geting working mvp prototype.
(prototype\_{timestamp}) with description of prototype structure + prototype code in src folder
