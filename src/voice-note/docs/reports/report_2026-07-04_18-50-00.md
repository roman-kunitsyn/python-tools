# voice-note TUI CSS Compatibility Fix

## Summary

Fixed the TUI stylesheet so it parses on the installed Textual version.

## Changes

- Removed unsupported `gap` properties from the main TUI stylesheet.
- Replaced spacing with supported margin/padding adjustments.

## Checks Run

- `python -m compileall src/voice-note/voice_note src/voice-note/tests`
- `PYTHONPATH=src/voice-note uv run python -m unittest discover -s src/voice-note/tests`

## Notes

- The workspace layout remains intact; only the CSS spacing syntax changed.
