This directory is the default config save location for `voice-note`.

The TUI Settings form saves `config.json` here by default, and you can load it
later with `--config src/voice-note/config/config.json`.
