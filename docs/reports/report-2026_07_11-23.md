# Report: Hourly Report Convention Update

timestamp: 2026-07-11 23:03:19

## Summary

Updated the shared report convention so agents write one hourly report file and
append new entries within the same hour.

## Files Changed

- `docs/guidelines/ARCHITECTURE_GUIDELINE.md`
- `docs/guidelines/IMPLEMENTATION_GUIDELINE.md`
- `docs/guidelines/PROJECT_DOCUMENTATION_GUIDELINE.md`
- `docs/improvements_plan.md`
- `docs/prompts/update_documentation.md`
- `docs/reports/report-2026_07_11-23.md`

## Changes Made

- Changed the report naming rule from one file per task timestamp to one file
  per hour.
- Defined the hourly report filename as `docs/reports/report-YYYY_MM_DD-HH.md`.
- Added the rule that multiple report entries created in the same hour should
  be appended to the same file.
- Added a full timestamp marker requirement inside each report entry.
- Updated the shared docs and prompt text so they all describe the same
  behavior.

## Checks Performed

- Reviewed the updated shared documentation files for consistency.
- Verified the new naming rule is reflected in the implementation guideline,
  project documentation guideline, architecture guideline, and prompt.

## Remaining Risks

- Existing historical report files still use the older per-task naming pattern.
- Any tools or scripts that automatically create reports will need to follow the
  new hourly file convention.

## commit_name

`hourly-report-convention`

---

# Report: voice-note CLI Session Prompt and Status Fix

timestamp: 2026-07-11 23:24:09

## Summary

Fixed the `voice-note --mode cli` session prompt and the live status rendering
so the terminal output is timestamped, colorized, and no longer leaves the
transcription lines indented.

## Files Changed

- `src/voice-note/voice_note/cli/cli_app.py`
- `src/voice-note/voice_note/main.py`
- `src/voice-note/README.md`
- `src/voice-note/docs/IMPLEMENTATION_PLAN.md`
- `src/voice-note/tests/test_voice_note_service.py`
- `docs/reports/report-2026_07_11-23.md`

## Changes Made

- Added a session-name prompt before CLI recording begins.
- Kept the default session title visible in gray/dim text so Enter accepts it.
- Switched the live CLI status output to Rich-styled timestamped lines.
- Cleared the active status line before printing transcription output to avoid
  the weird indentation.
- Added tests for the prompt helper and timestamped status rendering.
- Updated the `voice-note` README and implementation plan to describe the new
  CLI behavior.

## Checks Performed

- Ran `PYTHONPATH=src/voice-note uv run python -m unittest discover -s src/voice-note/tests`
- Confirmed the full `voice-note` test suite passes.

## Remaining Risks

- The prompt and status rendering are optimized for an interactive terminal;
  non-TTY invocation still falls back to the normal settings flow.
- Any future CLI formatting changes should preserve the raw clear-line write so
  the transcript output stays aligned.

## commit_name

`voice-note-cli-session-prompt-status-fix`

---

# Report: voice-note Plain Transcript Output Fix

timestamp: 2026-07-11 23:30:03

## Summary

Changed the CLI transcript body output to plain text so long notes stay flush
left without the hanging indentation caused by Rich wrapping.

## Files Changed

- `src/voice-note/voice_note/cli/cli_app.py`
- `src/voice-note/tests/test_voice_note_service.py`
- `src/voice-note/README.md`
- `src/voice-note/docs/IMPLEMENTATION_PLAN.md`
- `docs/reports/report-2026_07_11-23.md`

## Changes Made

- Switched the transcript text output from Rich-rendered text to a direct plain
  write on the console file handle.
- Kept the timestamped Rich status lines for session and transcription status.
- Added a regression test that checks the transcript body is written plainly.
- Updated the README and implementation plan to describe the plain transcript
  body output.

## Checks Performed

- Ran `PYTHONPATH=src/voice-note uv run python -m unittest discover -s src/voice-note/tests`
- Confirmed all `voice-note` tests pass after the rendering change.

## Remaining Risks

- Very long transcript lines will still wrap in the terminal, but they should
  now wrap naturally without the extra leading indentation.
- Future Rich formatting changes in the CLI should keep transcript text on the
  plain write path.

## commit_name

`voice-note-plain-transcript-output-fix`

---

# Report: voice-note Session Folder Link Fix

timestamp: 2026-07-11 23:37:01

## Summary

Changed the CLI session header to link to the full session folder name instead
of the short title.

## Files Changed

- `src/voice-note/voice_note/cli/cli_app.py`
- `src/voice-note/voice_note/main.py`
- `src/voice-note/tests/test_voice_note_service.py`
- `src/voice-note/README.md`
- `src/voice-note/docs/IMPLEMENTATION_PLAN.md`
- `docs/reports/report-2026_07_11-23.md`

## Changes Made

- Added a session-folder link to the CLI header.
- Displayed the full folder name, such as `voice_note_2026_07_11-23_31_13`,
  instead of just `voice_note`.
- Kept the link target pointed at the actual session directory.
- Added a regression test for the session-line link text and URI.
- Updated the README and implementation plan to describe the clickable folder
  header.

## Checks Performed

- Ran `PYTHONPATH=src/voice-note uv run python -m unittest discover -s src/voice-note/tests`
- Confirmed all `voice-note` tests pass after the session link change.

## Remaining Risks

- The session header depends on Rich hyperlink support in the terminal.
- If a terminal does not render hyperlinks, the full folder name still appears
  as text, but the click behavior may vary.

## commit_name

`voice-note-session-folder-link-fix`

---

# Report: voice-note Cursor Reset Fix

timestamp: 2026-07-11 23:38:30

## Summary

Adjusted the CLI transcript output so the cursor resets cleanly to the left
when the transcript body is printed.

## Files Changed

- `src/voice-note/voice_note/cli/cli_app.py`
- `src/voice-note/tests/test_voice_note_service.py`
- `src/voice-note/README.md`
- `src/voice-note/docs/IMPLEMENTATION_PLAN.md`
- `docs/reports/report-2026_07_11-23.md`

## Changes Made

- Switched the transcript body to `console.print(note.text)` instead of a raw
  file write.
- Kept the timestamped note header and Rich status lines intact.
- Added the left-reset behavior that prevents the cursor from staying offset
  after transcribing.
- Left the transcript body flush left while preserving wrapping behavior for
  long lines.

## Checks Performed

- Ran `PYTHONPATH=src/voice-note uv run python -m unittest discover -s src/voice-note/tests`
- Confirmed all `voice-note` tests pass after the cursor reset change.

## Remaining Risks

- Very long transcript lines may still wrap based on terminal width, but they
  should now wrap naturally from the left margin.
- Terminal hyperlink rendering still depends on terminal support.

## commit_name

`voice-note-cursor-reset-fix`

---

# Report: voice-note Plain Transcript Body Restore

timestamp: 2026-07-11 23:43:57

## Summary

Restored the transcript body to a plain raw write so the cursor stays at the
left margin after transcribing and the output does not pick up a hanging
indentation.

## Files Changed

- `src/voice-note/voice_note/cli/cli_app.py`
- `src/voice-note/README.md`
- `src/voice-note/docs/IMPLEMENTATION_PLAN.md`
- `docs/reports/report-2026_07_11-23.md`

## Changes Made

- Switched the transcript body output back to a direct raw write.
- Kept the timestamped note header and Rich status lines intact.
- Updated the README and implementation plan to describe the plain transcript
  body after the note header.

## Checks Performed

- Ran `PYTHONPATH=src/voice-note uv run python -m unittest discover -s src/voice-note/tests`
- Confirmed all `voice-note` tests pass after restoring the raw body write.

## Remaining Risks

- Very long transcript lines still depend on terminal width, but they should
  now start from the left margin without the previous offset.

## commit_name

`voice-note-raw-transcript-body-restore`
