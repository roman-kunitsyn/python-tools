# Report: Config Option Wording Clarification

## Summary

Clarified the precedence rule wording so it refers to named CLI option values
instead of a literal `--flag` placeholder.

## Files Changed

- `docs/guidelines/ARCHITECTURE_GUIDELINE.md`
- `docs/roles/TOOL_ENGINEER.md`
- `src/voice-note/docs/IMPLEMENTATION_PLAN.md`
- `docs/reports/report_2026-07-11_22-40-16.md`

## Changes Made

- Replaced ambiguous `--flag` wording with:
  - named CLI option values
  - CLI option values
- Kept the precedence rule intact:
  - CLI option value > `--config` value > built-in default
- Preserved the `voice-note` alignment plan wording using the same clearer
  terminology.

## Checks Performed

- Reviewed the affected docs for consistency after the wording change.
- Verified the new language matches actual options such as `--input` and
  `--output`.

## Remaining Risks

- If a module has unusual precedence rules, it may need its own local note.

## commit_name

`config-option-wording-fix`
